<!-- チェックリストの作成、編集フォーム -->

<%= form_for @project, html: {class: "form-horizontal" } do |p| %>
	<fieldset>
		<legend><%= t('labels.contents') %></legend>

		<!-- タイトル -->
		<div class="form-group">
			<%=p.label :title, class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%=p.text_field :title, class: "form-control", placeholder: t('activerecord.attributes.project.title_description') %>
			</div>
		</div>

		<!-- タグ -->
		<div class="form-group">
			<%=p.label :tags, class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= tag(:input, { id: "tokenfield", type: "text", name: "project[tag_list]", value: @project.tag_list.join(", "), class: "form-control", placeholder: t('activerecord.attributes.project.tags_description') }) %>
				<%= Check.all_tags.join(", ") %>
			</div>
		</div>

		<!-- 実行ボタン -->
		<div class="form-group" align="right">
			<div class="col-lg-10 col-lg-offset-2">
				<%= button_tag(type: 'submit', class: "btn btn-primary") do %>
					<i class="glyphicon glyphicon-save"></i> <%= t('labels.save')%>
				<% end %>
			</div>
		</div>

	</fieldset>

<% end %>

<script>
	$('#tokenfield').tokenfield()
</script>